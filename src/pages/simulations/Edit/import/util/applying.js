
// applyChanges takes a simulation and an Excel workbook and applies the changes from that workbook into the Excel file.
export function applyChanges(simulation, workbook) {
	window.alert('Diese Funktionalität ist noch nicht implementiert worden.')
	console.log(simulation, workbook)
}
